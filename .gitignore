# Node
node_modules/
npm-debug.log*

# Vite build output
client/dist/

# PocketBase — the binary itself (downloaded separately)
server/pocketbase
server/pocketbase.exe

# PocketBase runtime data — NEVER commit this
# pb_data contains your SQLite database, student records, everything
server/pb_data/

# PocketBase static serving — regenerated by build.sh
server/pb_public/

# Distribution zips — generated by package.sh
dist/

# OS
.DS_Store
Thumbs.db

# Environment files — may contain secrets
.env
.env.local
.env.*.local

# Cloudflare
.wrangler/
worker/node_modules/

Classic-32x-Java/
tmp

# Gradle
.gradle/
build/

# VS Code Java
.classpath
.project
.settings/
bin/

.env.local